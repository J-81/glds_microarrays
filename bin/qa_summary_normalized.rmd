---
author: "NASA GeneLab Project"
date: "`r Sys.Date()`"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE,
		      fig.width = 12, 
		      fig.height = 8,
		      error = TRUE # since endpoints include saved data, this is safe as requisite data not being saved will block workflow progression.  This allows a debugger to see the erroring Rmarkdown report.
		      )

# load libraries, could be moved to specific chunks if overloading becomes an issue, but easy to remove load related print outs in this chunk
library(limma)
library(oligo)

# load required objects from rData files
load("normalized.RData")
load("targets.RData") # named 'targets'

# set up common variables
count <- dim(targets$t1)[1]
```

# `r targets$glds` Normalized Data QA Summary
#### `r Sys.Date()`
#### Organism: `r targets$organism`
#### Platform: `r targets$platform` Microarray


## Normalized Data Class
```{r normalized-class-listing}
normalized_class <- class(normalized)
print(normalized_class)
```

## Mapping Appropriate QA Raw Protocol
``` {r flag-setting, error=FALSE}
flags <- c("expressionset_flag","malist_flag","elist_flag")
flag_state <- data.frame(row.names=flags, criteriaMet=rep(FALSE, times=length(flags)))
flag_state["expressionset_flag",] <- (is(normalized,"ExpressionSet")) # as generated by oligo readcel
flag_state["elist_flag", ] <- (is(normalized,"EList")) # as generated by one channel read.maimages
flag_state["malist_flag", ] <- (is(normalized,"MAList")) # as generated by two channel read.maimages

supported_flags <- sum(flag_state[,"criteriaMet"])

print(flag_state)
```

``` {r check-flags}
if (supported_flags != 1) { 
    stop(sprintf("Error: One and only one flag condition should be met. Flag conditions met: %d,  see flag conditions in this markdown file", supported_flags))
}
```

## Study Sample Factor Grouping
```{r study-sample-factor-grouping, echo=FALSE}
  DT::datatable(targets$t1)
```

## Normalized Data MA Plots
```{r featureset--ma-plots,  eval=(flag_state["expressionset_flag",]), include=(flag_state["expressionset_flag",])}
for (i in 1:count){
    plotted <- oligo::MAplot(normalized, which=i, pairs=FALSE, main="\nvs pseudo-median reference chip")
}
```

```{r lists--ma-plots,  eval=(any(flag_state[c("elist_flag","malist_flag"),])), include=(any(flag_state[c("elist_flag","malist_flag"),]))}
count <- dim(targets$t1)[1]
for (i in 1:count){
    limma::plotMD(normalized, column = i, xlab = "Average log-expression", ylab = "log-fold-change", main = colnames(normalized)[i])
  }
```

## Normalized Data Density Plot
```{r featureset--density-plot, eval=(flag_state["expressionset_flag",]),  include=(flag_state["expressionset_flag",])}

if (class(normalized)=="ExonFeatureSet" || class(normalized)=="GeneFeatureSet"){
  oligo::hist(normalized, main = paste0(targets$glds," Raw Intensity Distributions"), target="probeset",xlab="Intensity",ylab="Density",col=rainbow(count),lty=1)
  legend("topright",legend=targets$t1$SampleName,fill = rainbow(count))
}else{
  oligo::hist(normalized, main = paste0(targets$glds," Raw Intensity Distributions"),xlab="Intensity",ylab="Density",col=rainbow(count),lty=1)
  legend("topright",legend=targets$t1$SampleName,fill = rainbow(count))
}

```
```{r list--density-plot, eval=(any(flag_state[c("elist_flag","malist_flag"),])), include=(any(flag_state[c("elist_flag","malist_flag"),]))}
limma::plotDensities(normalized, legend = FALSE)
legend("topright",legend=targets$t1$SampleName,fill = rainbow(count))

```

## Normalized Data Box Plot
```{r featureset--box-plots, eval=(flag_state["expressionset_flag",]),  include=(flag_state["expressionset_flag",])}
longest_sample_name_length <- max(nchar(sampleNames(normalized))) * 0.5
bottom_margin <- min(35, longest_sample_name_length)
par(mar=c(bottom_margin,5,5,1))
if (class(normalized)=="ExonFeatureSet" || class(normalized)=="GeneFeatureSet"){
oligo::boxplot(normalized,which="pm",transfo=log2, nsample=10000,main=paste0(targets$glds," Normalized Data Log2 Intensities"), las=2,outline=FALSE,ylab = "log2 Intensities", target="probeset")
}else{
oligo::boxplot(normalized,which="pm",transfo=log2, nsample=10000,main=paste0(targets$glds," Normalized Data Log2 Intensities"), las=2,outline=FALSE,ylab = "log2 Intensities")
}
```
```{r list--box-plots, eval=(any(flag_state[c("elist_flag","malist_flag"),])), include=(any(flag_state[c("elist_flag","malist_flag"),]))}
longest_sample_name_length <- max(nchar(rownames(normalized$targets))) * 0.5
bottom_margin <- min(35, longest_sample_name_length)
par(mar=c(bottom_margin,2,1,1))
boxplot(data.frame(log2(as.matrix(normalized))),main="Normalized Data Log2 Intensities", las=2,outline=FALSE)
```

## Normalized Data PCA Plot
```{r extract-normalized-expression }
exp_normalized <- log2(exprs(normalized))
exp_normalized <- log2(as.matrix(normalized))
```

```{r pca-plot }
library(ggplot2)
PCA_normalized <- tryCatch({
	prcomp(t(exp_normalized), scale. = TRUE) # scaling will fail for low replicates
}, error = function(e) {
	print(paste("Scaling failed, retrying without scaling. Error Message: ", e))
	prcomp(t(exp_normalized), scale. = FALSE)
})
PCA_normalized <- prcomp(t(exp_normalized), scale. = FALSE)
percentVar <- round(100*PCA_normalized$sdev^2/sum(PCA_normalized$sdev^2),1)
sd_ratio <- sqrt(percentVar[2] / percentVar[1])
dataGG <- data.frame(PC1 = PCA_normalized$x[,1], PC2 = PCA_normalized$x[,2], Group = targets$t1$Group)
PCA_plot <- ggplot(dataGG, aes(PC1, PC2)) +
      geom_point(aes(colour = Group)) +
  ggtitle("Normalized Data PCA Plot of log2 Expression") +
  xlab(paste0("PC1, VarExp: ", percentVar[1], "%")) +
  ylab(paste0("PC2, VarExp: ", percentVar[2], "%"))
plot(PCA_plot)

write.csv(PCA_normalized$x, file = "visualization_PCA_table.csv")
```

