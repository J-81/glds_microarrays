params {
  outputDir = "."
  isa_archive = false // a prior downloaded isa archive, used instead of pulling from GeneLab repos
  publish_dir_mode = "link" // method for creating publish directory.  Default here for hardlink
}
